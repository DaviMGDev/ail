# specs-directory — Snapshot

**Time:** 2026-08-08 17:06

## Since last snapshot
User asked me to close ALL open questions, using the thinker subagent. Consultation: thinker-a (Q1-Q5) succeeded with complete verdicts; thinker-b (Q6-Q11) crashed twice with SIGABRT; I salvaged the first combined run's full 76KB thinking stream (which contained complete Q6-Q11 analysis and the Weakness A-H audit) from the transcript artifacts. All decisions are now made; nothing is left open.

## Current state
DECISIONS (final — all open questions closed):
- Purpose: ACL (Agent Controlled Language) = fork/superset of ACE 6.7; sequences/procedures; no tooling; pure design exercise; spec = artifact.
- Q1 Knowledge-section Otherwise: KEEP (total-case rules); continuation-only (must follow if-sentence or another Otherwise); punctuation disambiguates (declarative "." vs imperative "!"); "Otherwise if B then C." expands to "If not A and B then C." (negation-guarded chain).
- Q2 Call arguments: any entity-denoting NP (definites/indefinites/proper names/variables); quantified ("every card") and coordinated arguments FORBIDDEN (would smuggle iteration).
- Q3 While: condition re-evaluated each pass, fresh existentials per pass; fixed binding is degenerate; multiple matches = non-deterministic (documented).
- Q4 Failure semantics: MINIMAL — closed-world condition evaluation (unstated = false); impossible step aborts the run (anaphoric gap, undefined procedure, type mismatch); no recovery constructs.
- Q5 Queries: NOT steps; tests only in If/While conditions; top-level interrogatives remain valid (superset).
- Q6 Main sequence: ALLOWED — commands in written order; declaratives anywhere outside procedures = knowledge (initial world state, in force from start); bare Stop! in main sequence ends the run.
- Q7 Calls: implicit continue-after-return; Stop! terminates only current procedure; recursion allowed (fresh activation, non-termination = user error); nested definitions FORBIDDEN; redefinition FORBIDDEN (flat global namespace); parameter type annotation = constraint, mismatch = invalid text; parameter binds argument's referent; anaphora inside body = most-recent-antecedent with argument most recent at entry.
- Q8 Otherwise adjacency: MUST immediately follow its if-sentence; only "Otherwise if ..." chains intervene; same rule in knowledge section.
- Q9 Fiat: YES — ACL clarifies semantics, NEVER rejects syntax ACE accepts; Compatibility annex + conformance corpus of ACE texts.
- Q10 Sufficiency: six constructs sufficient for core claim; ONE essential semantic rule added: declarative steps in procedure bodies = ASSERTIONS updating the agent's world model (ACE-native replacement for assignment via anaphora+assertion). Omitted: return values, concurrency, timing, events, recovery, loop-break (expressible via While+conditions), Wait-until.
- Q11 Superset: no syntactic violations; semantic additions documented (ordering for multi-command texts); keyword carve-outs needed for Stop!/Procedure/Otherwise/While (position-restricted); procedure names MUST be hyphenated; call = name + "(" args ")" in command position; colon = new semantic token; indentation = body delimiter (procedure bodies only); loop bodies = EXACTLY ONE step (multi-step via procedures); if-arms = single step, no nested if (via procedures); While+Otherwise undefined = omitted.
- For-every semantics: SNAPSHOT at loop entry (deterministic); While = live re-check.
- World model section (Weakness A): THE required addition — world = objects + facts; knowledge seeds it; commands change it; conditions read it; closed world.
- Name: keep "Agent Controlled Language"; SPEC.md notes FIPA ACL homonym.
- Seams (a/some singular; "a richest customer"): closed by fiat per Q9 (spec states some = plural/mass only; superlatives take "the"; anything ACE accepts stays accepted).

OPEN QUESTIONS: NONE — all closed. Next step (outside discussion mode): write the ACL extension into specs/ (LANGUAGE.md extension section + SPEC.md reframe + Compatibility annex).

REASONING CHAINS:
- Thinker consultation: thinker-a completed with full verdicts (Q1-Q5). Part-2 runs (Q6-Q11) crashed twice (SIGABRT, no output) — salvaged the first combined run's 76KB thinking stream (6b30776b) which contained the complete Q6-Q11 analysis + Weakness A-H audit. Decisions Q6-Q11 synthesized by me from that reasoning.
- Weakness audit (from thinker): A world-state assumption implicit (weakest point → world model section); B For-every snapshot vs live (→ snapshot); C action-effects boundary (commands = world actions; calls recognized by name/hyphen); D anaphora across calls + type annotations (→ constraint); E otherwise-if chain asymmetry (minor, accepted); F while+otherwise (omitted); G wait-until (omitted); H superset untestable (→ conformance corpus + divergence annex).

KEY INSIGHTS:
- Declarative-assertion-as-step is the ACE-native answer to assignment: anaphora + assertion replaces variables.
- The fork's only true semantic addition is ORDER + an explicit world model; everything else is vocabulary/layout.
- Design criterion honored throughout: minimalism (each construct pulls more weight than its size).
