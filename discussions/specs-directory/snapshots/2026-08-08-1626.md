# specs-directory — Snapshot

**Time:** 2026-08-08 16:26

## Since last snapshot
First snapshot. Topic opened: "about @specs/" in ~/Projects/local/ontology/ace. I read SPEC.md, LANGUAGE.md, index.md, log.md. Starting position: the specs/ directory is a repurposed spec-md setup documenting ACE 6.7 (Attempto Controlled English) — a language, not a software system. SPEC.md is deliberately light (most sections marked not-applicable), LANGUAGE.md is the primary artifact (~10KB syntax reference). Status: draft. I asked two questions: purpose of the directory, and verification strategy for the unverified ACE claims.

## Current state
DECISIONS (tentative):
- Purpose of specs/: PERSONAL KNOWLEDGE BASE — a maintained reference for writing ACE texts; value stated as accuracy and completeness.
- Verification: user chose "Trust + expand instead" — current content is trusted as accurate enough; better use of time is covering more of the language. (Note: this is in mild tension with "accuracy" as a stated value — not pushed, user's call.)

OPEN QUESTIONS:
- Which expansion direction to prioritize (proposed: closed-class function-word inventory / ACE→OWL-SWRL mapping / troubleshooting & DRS reading — awaiting user pick).
- Whether the user actually authors OWL/SWRL ontologies via ACE (context: path is ~/Projects/local/ontology/ace) — unexplored territory, would determine value of an OWL/SWRL mapping section.
- What "complete" coverage of ACE 6.7 means for this KB, and how LANGUAGE.md stays navigable as it grows (single file vs. citizen extraction) — design tension, not yet discussed.
- Seam spotted: golden rule 7 says "a/some introduce new entities" but NP section only shows "some" with plural/mass nouns — does "some card" parse in ACE 6.7? Also "a richest customer" looks suspect (superlative + indefinite article). Type: factual, verification deferred by user.

REASONING CHAINS:
- Structure of specs/ is settled and working (SPEC.md light + LANGUAGE.md as primary artifact + MKF scaffolding). The action is in LANGUAGE.md's content.
- Since growth beats verification: the useful framing is "what does a writer need that the doc doesn't cover" — expansion should target writer-facing gaps, not just official-doc coverage.

KEY INSIGHTS:
- Every line of LANGUAGE.md is an empirical claim about APE; counterexamples marked * are the highest-risk claims (a wrong * teaches the wrong lesson).
- Function words in ACE are a closed, non-extensible set — a personal KB lacks the actual inventories (prepositions, pronouns, query words, modals), which is precisely what a writer cannot look up elsewhere.
