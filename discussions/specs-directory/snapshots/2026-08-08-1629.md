# specs-directory — Snapshot

**Time:** 2026-08-08 16:29

## Since last snapshot
User revealed the real intent behind specs/: it is groundwork for FORKING ACE into a new controlled language — "Agent Controlled Language" (ACL) — a superset of ACE adding sequences/procedures. No tooling/compiling is planned. This corrects my earlier picture (personal ACE knowledge base) — the directory is the seed of a new language definition. My three proposed expansion directions (closed-class inventories, OWL/SWRL mapping, troubleshooting) are now secondary: the primary work is designing the extension.

## Current state
DECISIONS (tentative, updated):
- Purpose of specs/ — CORRECTED by user: the real goal is to FORK ACE and create a new controlled language: "Agent Controlled Language" (ACL). ACL is a SUPERSET of ACE that additionally supports sequences/procedures.
- No compiler/checking tooling is planned — the intent is not to "compile" anything. (Implies: the spec documents are the normative definition of ACL.)
- Earlier answers (personal knowledge base; trust + expand) still stand for the ACE base content but are subsumed: ACE content is the trusted BASE; the real work is the extension.
- Structure of specs/ is settled for now (SPEC.md light + LANGUAGE.md primary + MKF scaffolding), but ACL may force reorganization (rename/reframe vs. separate extension doc) — OPEN.

OPEN QUESTIONS:
- What executes ACL texts? (LLM agent reading the spec? Humans? Nothing — pure design exercise?) — this determines required precision of the spec. Type: unexplored territory / value judgment. ASKED via question tool.
- Sequence semantics: is ACL still declarative at heart (procedures are defined structures) or imperative (steps execute in order with control flow)? Design tension — declarative base vs. imperative extension.
- Does the fork stay a strict superset (every valid ACE text is a valid ACL text)? Compatibility property worth preserving — unconfirmed.
- Cross-procedure anaphora/scope: can a procedure refer to entities from the calling context? Parameters/variables introduction rules? Type: unexplored.
- Failure semantics: ACE has no notion of step failure; an agent language likely needs success/failure handling in sequences. Type: unexplored.
- Name collision: FIPA ACL (Agent Communication Language, multi-agent systems) exists. Minor.
- Earlier seams (a/some with singular countables; "a richest customer") still open but deprioritized.

REASONING CHAINS:
- KEY INSIGHT: with no parser/compiler, the spec IS the implementation — "valid ACL" is defined solely by the docs. Whatever follows the doc (human or LLM) is the runtime. This raises the stakes on precision of the language definition, especially for the NEW constructs (sequences/procedures) which have no external reference to check against.
- KEY INSIGHT: ACE is a declarative axiom language (texts = unordered sets of sentences); sequences/procedures introduce order, control flow, and arguably state — this is the core design tension of the fork.
- The fork preserves ACE's readability + controlledness while dropping the formal-semantics commitment (no FOL/DRS target needed).

KEY INSIGHTS accumulated:
- Every line of LANGUAGE.md is an empirical claim; with no APE check, counterexamples (*) are the highest-risk lines.
- For ACL, the new constructs are the parts that need the most precise definition — there is no external authority to verify against.
