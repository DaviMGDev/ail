# specs-directory — Snapshot

**Time:** 2026-08-08 16:40

## Since last snapshot
User short-circuited the Socratic mode: doesn't care to decide the voice question, delegated design decisions to me, and asked for a COMPLETE proposal of what and how to add to the syntax, with the hard constraint that ACE texts stay valid ACL (superset). Also confirmed liking the if/then/otherwise idea. The discussion pivots from exploration to delivery: I produce the full extension design now.

## Current state
DECISIONS (updated):
- Purpose: fork ACE into ACL (Agent Controlled Language) — superset; sequences/procedures + whatever supporting constructs are needed.
- No tooling; pure design exercise; the spec is the artifact.
- USER DELEGATED DESIGN DECISIONS: "I don't care too much about what you decide... I just want complete suggestions of what and how to add." → I am authorized to take positions and produce a COMPLETE extension proposal. User explicitly wants the full design now, not a Socratic discussion.
- HARD REQUIREMENT from user: ACE texts must remain valid ACL (strict superset, "keeping ACE working as intended").
- Branching idea (if/then/otherwise) confirmed by user: "oh, the idea is if/then/otherwise? I like it."

OPEN QUESTIONS (now mostly mine to resolve in the proposal):
- Voice of control section (imperative vs declarative) — I resolved: imperative steps with implicit addressee (agent) in control contexts.
- Procedure syntax, parameters, calls — to be proposed.
- Loops — to be proposed (For every / While).
- State/return values — decided: omit (no state; no return values; tests via branching) — noted as extension point.
- Scope rules for procedure-local entities — to be proposed.

REASONING CHAINS:
- If/Otherwise has uniform meaning (complementary case) in both knowledge (total-case rules) and control (branch) contexts; chains via "Otherwise if".
- ACE's declarative/command split prefigures knowledge/control split.
- Minimalism criterion: each new construct should pull more weight than its size; everything must obey inherited golden rules.
- Sequence semantics = the only true semantic addition (order matters in control contexts); everything else is syntax/grammar extension.
- Implicit-addressee commands in control contexts is the smallest rule enabling imperative steps without "Agent," on every line.

KEY INSIGHTS:
- No parser → spec is the definition; internal consistency is the quality bar.
- "Otherwise" is the one-word completion of ACE's half-finished if-then.

USER CORRECTIONS INTEGRATED:
- Not a personal ACE KB primarily — it's the seed of ACL.
- Not a Socratic exploration — user wants the complete design proposal delivered, decisions made by me.
