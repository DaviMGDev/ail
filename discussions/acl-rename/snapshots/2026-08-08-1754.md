# acl-rename — Snapshot

**Time:** 2026-08-08 17:54

## Since last snapshot
User chose AIL (Agent Instruction Language) as the new name, then chose a clean rename with no "formerly ACL" note. Discussion complete: name decided, style decided, no open questions.

## Current state
DECISIONS (final — discussion closed):
- Language renamed: ACL (Agent Controlled Language) → **AIL (Agent Instruction Language)**.
- Rename style: CLEAN — no "formerly ACL" note in the docs; no historical footnote.
- Rationale: FIPA ACL homonym conflict is semantic (both agent languages); "instruction" matches the language's substance (commands, procedures, Stop! = an instruction set); keeps Agent framing; acronym clean in AI and linguistics contexts. Rejected: APL (programming language), ALC (description logic), AAE (African American English), APE (Attempto Parsing Engine), SCE (Sony), ECE (early childhood ed), PCE/CAPE (dropped by user choice).

OPEN QUESTIONS: NONE.

IMPLEMENTATION SCOPE (for after discussion mode — not executed):
- specs/acl.md → specs/ail.md (file rename; frontmatter title/description update; content: name, examples, "ACL" mentions → AIL).
- specs/SPEC.md: title/description/frontmatter, Context (ACL→AIL + drop FIPA naming note), US-002 wording, Decisions (ACL→AIL), API section.
- specs/index.md: registry row + reading order.
- specs/log.md: log the rename event (required by charter; entry states the change, minimal).
- AGENTS.md: layout description mentions acl.md → ail.md.
- discussions/ and git history: untouched (records, not rewritten).
- Commit per AGENTS.md conventions: docs(specs): rename ACL extension to AIL.
